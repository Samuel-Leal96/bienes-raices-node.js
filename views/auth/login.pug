extends ../layout/index

block contenido
    div.py10
        h1(class = "text-4xl my-10 font-extrabold text-center") Bienes
            span(class = "font-normal") Raices
        h2.text-center.text-2xl.font-extrabold= pagina

        //*Mostrar errores
        if errores
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold")= error.msg

        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow")
                form(method="POST" action="/auth/login" class="space-y-5")
                    input(type="hidden" name="_csrf" value=csrfToken)
                    div
                        label(class="block text-sm uppercase text-gray-600 mb-2 font-bold" for="email") Email de registro
                        input#email(
                            type="email" 
                            name="email"
                            class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" 
                            placeholder="Tu correo electrónico")
                    div
                        label(class="block text-sm uppercase text-gray-600 mb-2 font-bold" for="password") Password
                        input#password(
                            type="password" 
                            name="password"
                            class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" 
                            placeholder="Tu contraseña")

                    div(class="flex items-center justify-between")
                        a(href="/auth/registro" class="text-gray-500 text-xs") ¿No tienes cuenta? Crea una nueva 
                        a(href="/auth/olvide-password" class="text-gray-500 text-xs") Olvide mi contraseña 

                    input(type="submit" value="Iniciar sesión" class="w-full bg-indigo-600 hover:bg-indigo-800 text-white font-bold py-2 cursor-pointer rounded-lg")